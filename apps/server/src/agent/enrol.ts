import type { Router } from 'express';
import type { ServiceDeps } from '../deps.js';

// Stub (SHP-P-2 pre-flight). SHP-T-2.2 replaces it.
export function mountEnrol(_router: Router, _deps: ServiceDeps): void {
  // nothing yet
}
