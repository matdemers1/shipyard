import type { Router } from 'express';
import type { ServiceDeps } from '../deps.js';

// Stub (SHP-P-2 pre-flight). SHP-T-2.4 replaces it.
export function mountPoll(_router: Router, _deps: ServiceDeps): void {
  // nothing yet
}
