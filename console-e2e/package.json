{
  "name": "console-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright against the built server serving the built console: every screen's states, and axe in both themes",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@playwright/test": "^1.63.0",
    "axe-core": "4.12.0",
    "otpauth": "^9.5.2"
  }
}
