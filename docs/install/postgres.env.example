# Copy to postgres.env, mode 600, and replace <random> with a fresh secret
# (head -c 32 /dev/urandom | od -An -tx1 | tr -d ' \n'). Use the SAME value in server.env's
# DATABASE_URL below.
POSTGRES_USER=shipyard
POSTGRES_DB=shipyard
POSTGRES_PASSWORD=<random>
