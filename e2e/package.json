{
  "name": "e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Docker-in-Docker, registry:2, fake GitHub and the toy app — real agent, real compose",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:unit": "vitest run tests/fake-github-logic.test.ts",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@shipyard/schema": "workspace:*",
    "@shipyard/sequence": "workspace:*",
    "yaml": "^2.9.1"
  }
}
