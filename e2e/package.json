{
  "name": "e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Docker-in-Docker, registry:2, fake GitHub and the toy app — real agent, real compose",
  "type": "module",
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "yaml": "^2.9.1"
  }
}
